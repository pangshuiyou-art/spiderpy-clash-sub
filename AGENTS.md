# AGENTS.md - AI Agent 速查指南

本文件为 AI Agent（Claude、GPT、Cursor、Gemini、Cline、Trae、WorkBuddy 及未来的任何 agent）提供**速查核心约束**，详细编码标准见 [CLAUDE.md](./CLAUDE.md)。

**⚠️ 重要提醒**：Agent 必须在本次会话全程遵守本文件约束，N 轮对话后仍需定期自查（见末尾"约束恢复提醒"）。

**📌 同步纪律**：本文件 ★ 四节硬规定（回复语言 / 回复结构 / 文件存放 / 记忆协议）为**权威源**，`CLAUDE.md` 内同名节为镜像副本；修改任一份时必须同步另一份，禁止两份各自漂移。

---

## ★ 回复语言硬性规定（最高优先级）

1. **所有回复一律使用简体中文**，禁止使用其他语言（含英文、繁体中文）。
2. 代码注释、文档、commit message 必须使用中文。
3. 用户用任何语言提问，回复仍用中文；仅在保留技术名词、代码标识符、文件路径、API 名称时可混入英文。
4. **优先级：本规定 > 本文件其余章节 > 用户个人偏好 > 通用最佳实践。**
5. 回复前自检一遍：确认全文为简体中文后再输出；违反即视为不合规，需立即纠正重写。

---

## ★ 回复结构硬性规定（强制）

1. 任何回复必须**先用大白话总结**：用通俗、直白、非技术化的语言，在 3~5 句话内给出核心结论与要点，让不懂技术的人也能看懂。
2. 总结之后**再进入详细解析**：逐步展开技术依据、实现细节、代码示例等。
3. 固定结构：**结论先行 → 大白话解释 → 技术细节**，禁止只堆技术细节、不给结论。
4. 除非用户明确要求"直接给方案 / 只看代码 / 无需解释"，否则不得跳过总结直接展开细节。
5. 大白话与详细解析需保持结论一致，不得前后矛盾。

---

## ★ 文件存放硬性规定（强制）

1. **项目交付物禁止落在系统盘（C 盘）**：所有新建文件、输出文件、临时文件、测试产物、日志等，一律只能放在当前项目目录内。
2. **例外**：各工具自管的运行态目录（如 `~/.claude/`、`~/.zcode/`、`~/.trae-cn/` 等）不受上条限制；但其内容**不得作为长期记忆**（长期记忆唯一事实源为 `.memory/`）。
3. 若发现已产生在 C 盘的项目文件，必须立即清理删除，并将有效内容转移到项目目录内。
4. 分类归档存放，禁止文件、目录散落：
   - 测试文件 → `tests/` 目录
   - 文档类文件 → `docs/` 目录
   - 其余产物 → 按职责在项目目录内建对应子目录归档（如 `src/`、`config/`、`scripts/`、`data/`、`logs/` 等）。
5. 存放规则：项目根目录 → 按类型归档到对应子目录；禁止散落根目录、随意新建目录或写入系统盘（C 盘）及其它盘符根目录。
6. 所有分析 / 生成结果文件（报告、修复方案、验收文档等）默认放在 `docs/` 或对应子目录，防止 C 盘膨胀与根目录散落。
7. 运行时调试产物（如浏览器调试目录、临时截图）生成本项目内，归属归档目录，不留在根目录。

---

## ★ 记忆协议（必读必守）

> 所有在本项目协作的 AI Agent **必须**遵循下列记忆读写规则，全文见 [.memory/README.md](./.memory/README.md)。

1. **会话开始第一步**：读 [.memory/MEMORY.md](./.memory/MEMORY.md)（索引总览）；领取/认领任务另读协作看板（如 `docs/任务看板.md`）。
2. **长期记忆唯一事实源**为 `.memory/`。**禁止**把长期项目记忆写入各软件私有目录当作长期记忆（如 `~/.trae-cn/`、`~/.claude/`、`~/.workbuddy/` 等），这些仅作运行态中间层。
3. **写入格式**：一条一文件 → frontmatter（`name/description/type/created/updated`）→ 放入对应子目录（`user/`、`project/`、`learnings/`）→ 更新 `MEMORY.md` 索引 → `git commit`。
4. **提交纪律**：commit 前先 `git pull --rebase` 防多 agent 冲突；`MEMORY.md` 索引**只允许追加自己条目对应的行、禁止改动他人行**（重排/清理由单一维护者定期执行），冲突时 rebase 后重试。
5. **保密**：**禁止**在记忆条目写入 token / 密码 / 密钥（与项目 `.env`、`data/settings.json` 等敏感文件红线同源）。
6. 任务认领 / 状态走协作看板，**不**写入 `.memory/`；`.memory/` 只在索引中引用看板。

---

## 项目身份
<!-- 按项目实际填写，删除本注释。这是"怎么写项目身份"的示例骨架 -->

- **项目**：{{项目名}}（{{项目一句话简介}}）
- **版本**：{{版本号，如 v1.0.0}}
- **形态**：{{项目形态，如 CLI / 桌面应用 / Web 应用}}
- **源项目**：{{若有重构复刻来源，填名称与只读引用状态；无则删除本行}} ⭐⭐⭐
- **核心创新/关键点**：{{1~2 句，让 agent 一上来就抓住项目灵魂；无则删除本行}}

---

## 快速上手
<!-- 按项目实际调整命令；删除不适用的行。这是示例骨架 -->
<!-- 默认按 Python 项目提供；非 Python 项目请替换本节命令与 CLAUDE.md §4 编码标准 -->

> 格式化与检查规则已由 `pyproject.toml.sample` 落地为工具配置（行长 120、不强制双引号），
> 复制模板后改名为 `pyproject.toml` 即生效，避免文档规则与工具默认行为打架。

```bash
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# 代码检查
ruff check .                     # Lint
black --check .                  # 格式检查

# 测试
pytest                           # 单元测试
pytest tests/unit -v             # 指定测试
pytest --cov={{包名}}              # 测试覆盖率
```

---

## 核心约束（红线，必须遵守）⭐⭐⭐
<!-- 按项目实际填写；每项约束必须写"原因"，让 agent 知其为啥锁。这是示例骨架 -->

### 1. 技术栈锁定


| 约束 | 原因 | 位置 |
|------|------|------|
| {{Python 3.12.x}} | {{固定版本，禁止擅自变更}} | {{requirements.txt}} |
| {{关键依赖 A x.y.z}} | {{为什么锁，例如"禁用内置重试，统一用项目策略"}} | {{requirements.txt}} |
| {{框架 B x.y.z}} | {{为什么锁}} | {{锁定位置}} |

### 2. 分层依赖方向

```
{{接口层 → 服务层 → 核心层 → 数据层 → 基础设施}}
{{api/    services/  core/    db/      common/config}}
```

**禁止逆向依赖**：
- {{核心层（`core/`）不得引用接口层（`api/`）或服务层（`services/`）}}
- {{数据层（`db/`）不得引用业务层（`services/`）}}
- {{接口层（`api/`）不直接调用 `core/`，必须通过 `services/`}}

### 3. 项目专属红线
<!-- 业务级红线示例骨架：每条给"禁止/允许"+原因。模板正文不含任何具体业务，全部以占位符示意 -->
<!-- 例：源项目只读保护 → 启用 docs/附录A_源项目只读保护.md 并按其配置校验脚本 -->
- ❌ {{禁止事项 1（如修改 {{只读目录}} 任何文件）}}
- ❌ {{禁止事项 2}}
- ✅ {{允许事项 1（如复制核心文件到本项目，原文件不动）}}
- ✅ {{允许事项 2}}
- 启用说明：{{若此红线有配套校验脚本/流程，写清阶段前后如何验证}}

### 4. 敏感数据与凭证
<!-- 按项目实际补充；模板默认列通用最小集 -->
- **禁止**提交 `data/settings.json`、`.env`、`*.log`、`*.db`、`*.sqlite`、`*.key`、`*.pem` 到 Git。
- 日志与报错中禁止打印完整 token / 密码 / 密钥，需脱敏（如 `token=abc...xyz`）。

---

## 禁止事项（红线清单）

- ❌ 擅自升级核心依赖版本（{{技术栈表锁定的版本}}）
- ❌ 违反分层依赖（{{接口层直接调核心层等}}）
- ❌ 修改 {{只读目录}} 任何文件（只读引用）
- ❌ 提交 `data/settings.json`、`*.log`、`.env`、`*.db`、`*.sqlite`、`*.key`、`*.pem`
- ❌ `git push --force` 到主分支
- ❌ 日志打印完整 token/密码（需脱敏：`token=abc...xyz`）
- ❌ 使用 `eval()` / `exec()`（除可信 JSON 解析）
- ❌ 嵌套三元运算符（用 if/else 或 match/case）
- ❌ 裸 `except` 吞掉所有异常
- ❌ 使用无意义缩写（`usr`、`cfg`、`tmp`、`btn`）
- ❌ 函数缺类型注解
- ❌ 函数嵌套 > 3 层
- ❌ 引用或修改 `{{_deprecated 等弃用目录}}` 目录代码

---

## Git 操作纪律（必须遵守）

### 危险操作（禁止 Agent 主动执行，必须用户明确确认）

- ❌ `git stash` / `git stash drop` / `git stash clear`
- ❌ `git reset --hard`
- ❌ `git clean -fd`
- ❌ `git checkout -- .`（批量丢弃）
- ❌ `git branch -D`（强制删除分支）
- ❌ `git push --force` / `git push -f`
- ❌ `rm -rf` 任何目录
- ❌ `{{taskkill /F /IM 等进程强制终止}}`

### 允许直接执行（常用只读/可控操作）

- ✅ `git status` / `git log` / `git diff`（只读）
- ✅ `git add <file>`（指定文件，不用 -A）
- ✅ `git commit -m "type(scope): subject"`
- ✅ `git reset --soft HEAD~1`（保留修改到暂存区）
- ✅ `git checkout -b <new-branch>`
- ✅ `git revert <commit>`（创建反向提交）
- ✅ `git pull --rebase`（提交记忆前必做）

---

## 执行强制检查（每次修改前必检）

**Agent 必须在每次修改代码前自检以下约束**：

```markdown
✅ 执行约束检查：
- [ ] 未违反分层依赖（{{接口层不直接调核心层，必须通过服务层}}）
- [ ] 未违反只读保护（不修改 {{只读目录}} 任何文件）
- [ ] 未违反技术栈锁定（{{锁定依赖}} 版本未变）
- [ ] 未违反敏感数据约束（不提交 settings.json / .env / *.log / *.db / *.key）
- [ ] 未违反日志脱敏约束（不打印完整 token/密码）
- [ ] 所有新增函数都有显式类型注解
- [ ] 文件已按类型归档（tests/ docs/ scripts/ data/ 等，不留根目录）
```

---

## 测试纪律（强制行为）

**测试失败时，Agent 必须**：
1. **立即停止当前任务**
2. **报告失败原因**（完整错误信息——注意：若错误信息含 token / 密码，须先脱敏再报告）
3. **等待用户指示**（禁止继续修改其他文件）
4. **修复后重新运行测试**（直到通过才能提交）

---

## 提交规范

```
<type>(<scope>): <subject>

type: feat | fix | refactor | docs | test | chore | perf
scope: <按项目填写，如 account | login | collection | ui | config | core>
subject: 祈使句，≤ 50 字符，不加句号
```

示例：
- `feat(login): {{适配某方案自动化登录}}`
- `fix(collection): {{修复某字段提取空值问题}}`
- `refactor(account): {{提取某逻辑到独立函数}}`

### 提交前检查

- [ ] 测试通过（`pytest`）
- [ ] Lint 通过（`ruff check .`）
- [ ] 格式正确（`black --check .`）
- [ ] 未提交敏感数据（`data/settings.json` / `.env` / `*.log` / `*.db` / `*.key`）
- [ ] {{只读目录}} git 状态干净（若启用只读保护）
- [ ] 提交信息符合规范

---

## 常用模块速查
<!-- 按项目实际填写；让 agent 一上来就知道改哪层、调哪个文件。这是示例骨架 -->

| 模块 | 职责 |
|------|------|
| `{{包名}}/{{核心模块}}` | {{一句话职责}} |
| `{{包名}}/services/` | {{业务编排层}} |
| `{{包名}}/{{采集器目录}}` | {{采集器}} |
| `{{包名}}/{{数据层目录}}` | {{Repository 实现}} |
| `{{包名}}/config/` | {{配置集中管理}} |
| `{{包名}}/common/` | {{日志、异常、重试、工具函数}} |

---

## 常见陷阱（AI 易犯错点）

> 模板内为通用示例。**项目必须把自身最常踩的坑、已修过的 bug（查 .memory/learnings）补到本节**，❌/✅ 对照可大幅提升遵守率。

### Git 破坏性操作

```bash
# ❌ 错误：为"清理环境"直接 stash / reset --hard
git stash && git reset --hard HEAD

# ✅ 正确：只做只读操作；确需丢弃时必须先问用户
git status && git diff
git checkout -b new-branch   # 需要隔离环境时开新分支
```

### 裸 except 吞异常

```python
# ❌ 错误
try:
    do_something()
except:
    pass

# ✅ 正确：精准捕获并给出中文说明
try:
    do_something()
except ValueError as exc:
    logging.warning(f"参数不合法: {exc}")
```

### 敏感信息入日志

```python
# ❌ 错误：打印完整凭证
logger.info(f"登录成功, cookie={cookie}")

# ✅ 正确：脱敏后再打印
logger.info(f"登录成功, cookie={mask_sensitive(cookie)}")
```

### 修改只读源项目

```text
# ❌ 错误：直接改 {{只读目录}} 里的文件
# ✅ 正确：复制到本项目后修改副本，原文件不动
```

---

## 约束恢复提醒（N 轮对话后自查）

**当会话进行到第 N 轮时，Agent 必须重新检查以下核心约束**（应对长会话上下文稀释）：

### 每 5 轮自查清单（强制）

```markdown
✅ 第 N 轮约束自查：
- [ ] 仍未违反分层依赖（{{接口层不直接调核心层，必须通过服务层}}）
- [ ] 仍未违反只读保护（不修改 {{只读目录}} 任何文件）
- [ ] 仍未违反技术栈锁定（锁定依赖版本未变）
- [ ] 仍未违反敏感数据约束（不提交 settings.json / .env / *.log / *.db / *.key）
- [ ] 仍未违反 Git 危险操作禁令（不主动 stash / reset --hard / clean -fd / branch -D）
- [ ] 所有函数都有显式类型注解
- [ ] 所有修改前都执行了强制检查
```

### 会话结束前最终检查（强制）

```markdown
✅ 会话最终检查：
- [ ] 所有修改测试通过（pytest）
- [ ] Lint 通过（ruff check .）
- [ ] 格式正确（black --check .）
- [ ] 未遗留未提交的敏感数据
- [ ] 未遗留未修复的测试失败
- [ ] {{只读目录}} git 状态干净（若启用只读保护）
- [ ] 所有提交信息符合规范
- [ ] 有价值的过程/结论已按记忆协议沉淀到 .memory/（若本次会话有可沉淀内容）
```

---

## 详细规范引用（必读）

- **编码标准**：[CLAUDE.md - 编码标准](./CLAUDE.md)
- **记忆协议全文**：[.memory/README.md](./.memory/README.md)
- **(可选附录) 源项目只读保护**：[docs/附录A_源项目只读保护.md](./docs/附录A_源项目只读保护.md)

---

**权威来源**：[CLAUDE.md](./CLAUDE.md) 为项目编码标准唯一权威来源，本文件仅提供速查约束。
**最后更新**：2026-08-26（每次修订后更新）