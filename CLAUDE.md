# CLAUDE.md — AI 编码助手权威指南

> **本文件为项目编码标准唯一权威来源**，AI 编码助手在本项目工作时的权威指南。
> 复制到任意项目后，将 `{{项目名}}`、`{{项目简介}}` 替换为实际内容，并按项目实际填写技术栈、目录结构、红线。
> 优先级：本文件 > 个人偏好 > 通用最佳实践。
>
> **📌 同步纪律**：本文件 ★ 四节硬规定（回复语言 / 回复结构 / 文件存放 / 记忆协议）为 `AGENTS.md` 的**镜像副本**（权威源：`AGENTS.md`）；修改任一份时必须同步另一份。本文件的权威性仅针对编码标准（§1~§5）。

## ★ 回复语言硬性规定（最高优先级）

1. **所有回复一律使用简体中文**，禁止使用其他语言（含英文、繁体中文）。
2. 代码注释、文档、commit message 必须使用中文。
3. 用户用任何语言提问，回复仍用中文；仅在保留技术名词、代码标识符、文件路径、API 名称时可混入英文。
4. **优先级：本规定 > 本文件其余章节 > 用户个人偏好 > 通用最佳实践。**
5. 回复前自检一遍：确认全文为简体中文后再输出；违反即视为不合规，需立即纠正重写。

---

## ★ 回复结构硬性规定（强制）

1. 任何回复必须**先用大白话总结**：用通俗、直白、非技术化的语言，在 3~5 句话内给出核心结论与要点，让不懂技术的人也能看懂。
2. 总结之后**再进入详细解析**：逐步展开技术依据、实现细节、代码示例等。
3. 固定结构：**结论先行 → 大白话解释 → 技术细节**，禁止只堆技术细节、不给结论。
4. 除非用户明确要求"直接给方案 / 只看代码 / 无需解释"，否则不得跳过总结直接展开细节。
5. 大白话与详细解析需保持结论一致，不得前后矛盾。

---

## ★ 文件存放硬性规定（强制）

1. **项目交付物禁止落在系统盘（C 盘）**：所有新建文件、输出文件、临时文件、测试产物、日志等，一律只能放在当前项目目录内。
2. **例外**：各工具自管的运行态目录（如 `~/.claude/`、`~/.zcode/`、`~/.trae-cn/` 等）不受上条限制；但其内容**不得作为长期记忆**（长期记忆唯一事实源为 `.memory/`）。
3. 若发现已产生在 C 盘的项目文件，必须立即清理删除，并将有效内容转移到项目目录内。
4. 分类归档存放，禁止文件、目录散落：
   - 测试文件 → `tests/` 目录
   - 文档类文件 → `docs/` 目录
   - 其余产物 → 按职责在项目目录内建对应子目录归档（如 `src/`、`config/`、`scripts/`、`data/`、`logs/` 等）。
5. 存放规则：项目根目录 → 按类型归档到对应子目录；禁止散落根目录、随意新建目录或写入系统盘（C 盘）及其它盘符根目录。
6. 所有分析 / 生成结果文件默认放在项目目录内，防止 C 盘膨胀。

---

## ★ 记忆协议（必读必守）

> 全文见 [.memory/README.md](./.memory/README.md)，索引见 [.memory/MEMORY.md](./.memory/MEMORY.md)。

1. **会话开始**先读 `.memory/MEMORY.md`；存在协作看板时，任务认领先读看板。
2. 长期记忆唯一事实源为 `.memory/`，**禁止**写入私有目录当长期记忆。
3. 写记忆：一条一文件 → frontmatter → 对应子目录 → 更新索引 → `git commit`（commit 前 `git pull --rebase`）。
4. **禁止**写入 token/密码/密钥。

---

## 1. 项目概览

- **项目名**：`{{项目名}}`
- **简介**：`{{项目简介}}`
- **版本**：`v0.1.0`（按实际填写）

### 1.1 核心功能

| 模块 | 说明 |
|------|------|
| {{模块一}} | {{说明}} |
| {{模块二}} | {{说明}} |

### 1.2 关键技术决策

- {{决策一：如"从源项目复用核心文件的零改动复用策略"}}
- {{决策二：如"分层 + 模板方法 + Repository 模式"}}

---

## 2. 技术栈与版本约束

> 每项必须写"为什么锁"（约束 / 原因 / 位置），只写版本不写原因，agent 就不知道能不能动。

| 项 | 版本 | 原因（为什么锁） | 位置 |
|----|------|------------------|------|
| {{语言}} | {{版本}} | {{固定版本，禁止擅自变更}} | {{requirements.txt 等}} |
| {{依赖 A}} | {{版本}} | {{如"禁用内置重试，统一使用项目指数退避策略"}} | {{requirements.txt}} |
| {{框架 B}} | {{版本}} | {{后端/前端框架}} | {{锁定位置}} |

---

## 3. 目录结构与分层

```
{{项目根目录}}/
├── main.py                # {{程序入口}}
├── {{包名}}/              # 主包
│   ├── api/               # {{接口层（路由）}}
│   ├── core/              # {{核心层}}
│   ├── services/          # {{服务层（业务编排）}}
│   ├── db/                # {{数据层（Repository 模式）}}
│   ├── common/            # {{公共工具}}
│   ├── config/            # {{配置集中管理}}
│   └── ...                # {{按项目实际补充}}
├── tests/                 # 测试（tests/unit、tests/integration、tests/conftest.py）
├── scripts/               # 脚本（按职责建子目录：verify/、migrate/、deploy/ 等）
├── docs/                  # 文档
└── data/                  # 运行时数据（进 .gitignore）
```

**分层依赖方向**：{{接口层 → 服务层 → 核心层 → 数据层 → 基础设施}}

**禁止逆向依赖**：
- {{核心层（`core/`）不得引用接口层（`api/`）或服务层（`services/`）}}
- {{数据层（`db/`）不得引用业务层（`services/`）}}
- {{接口层（`api/`）不直接调用 `core/`，必须通过 `services/`}}

---

## 4. 编码标准（强制）

<!-- 默认按 Python 项目提供（PEP 484 / snake_case）；非 Python 项目请整节替换，并在 AGENTS.md 快速上手中同步替换命令 -->

### 4.1 通用规则

- **缩进**：4 空格，禁止 Tab
- **引号**：单引号 `'`（字符串优先用单引号）
- **行尾**：禁用分号
- **换行符**：LF（`\n`）
- **文件编码**：UTF-8 无 BOM
- **行长**：≤ 120 字符

> 以上格式规则已由工具配置落地，复制模板后即生效：`pyproject.toml.sample`（ruff / black：行长 120、`skip-string-normalization` 保留单引号，改名 `pyproject.toml`）、`.editorconfig`（缩进 / LF / UTF-8）、`.gitattributes`（git 层强制 LF）。

### 4.2 命名约定

| 类型 | 约定 | 示例 |
|------|------|------|
| 变量 | snake_case | `user_id`、`config_file` |
| 函数 | snake_case | `get_product_info` |
| 类 | PascalCase | `LoginService`、`AccountManager` |
| 常量 | UPPER_SNAKE_CASE | `MAX_RETRY_TIMES` |
| 模块文件 | snake_case | `account_service.py` |

**禁止无意义缩写**：`usr`→`user`、`cfg`→`config`、`tmp`→`temp`、`btn`→`button`

### 4.3 函数定义与类型注解

所有函数必须显式类型注解（PEP 484）：

```python
def get_info(item_id: str) -> dict[str, str]:
    """获取信息"""
    return {}
```

### 4.4 导入顺序

```python
# 1. 内置模块
import os
from pathlib import Path

# 2. 第三方库
from fastapi import FastAPI
from pydantic import BaseModel

# 3. 本地模块
from {{包名}}.services.demo_service import DemoService
```

### 4.5 错误处理

优先预判拦截，避免不必要的 try/except。精准捕获异常，禁用裸 except。

### 4.6 模块化设计原则

- **单一职责**：每个模块/文件/函数只负责一个明确职责
- **接口稳定**：模块间通过明确接口通信
- **可扩展性**：新增功能只需新增子类（OCP）
- **模板方法模式**：{{采集器/上传器/检测器等}}使用 Base 抽象类
- **Repository 模式**：数据访问通过 Repository 接口

---

## 5. 项目专属红线（按项目填写）

<!-- ============ 红线写法示例（复制到新项目后，按实际替换，删除本节示例骨架） ============ -->

> **怎么写专属红线**：每条线给「禁/允」清单 + 触发验证。以下为"从源项目重构复刻"与"安全敏感"两个最常见场景的骨架，可整节替换、增删。

### 5.1 源项目只读保护（示例骨架，按需启用）

> 适用：本项目从另一项目（{{源项目}}）重构/复刻而来，源项目文件必须零修改。
> 完整启用步骤（含校验脚本模板）见 [附录A_源项目只读保护.md](./docs/附录A_源项目只读保护.md)。

- ❌ 修改 {{源项目}} 任何 `.py` / `.json` / `.md` / `.txt` 文件
- ❌ 在 {{源项目}} 目录创建任何新文件
- ❌ 删除或重命名 {{源项目}} 任何文件
- ❌ 对 {{源项目}} 仓库执行 git stash / reset / clean
- ✅ 只读读取源代码、配置、数据文件
- ✅ 复制核心文件到本项目（复制后源文件不动）
- 阶段开始前：`python scripts/verify/verify_readonly.py --before`
- 阶段结束后：`python scripts/verify/verify_readonly.py --after`

### 5.2 核心资产完整性保护（示例骨架）

> 适用：项目有"从源项目复制/不可随便改"的核心资产时的红线。按实际替换。

- {{配置开关}}保持 `{{特定值}}` 不变
- {{N 个核心文件}}复制后**零改动**
- {{某关键流程}}前必须调用 {{某方法}}

### 5.3 安全红线（示例骨架）

#### 凭证加密（按实际）

- {{加密方案说明，如"双层密钥：KEK（DPAPI包裹）→ DEK（AES-256-GCM）"}}
- 密钥不落盘，{{KeyManager}} 类管理密钥版本
- `{{encryption_version}}` 字段兼容期（0=明文，1=加密）

#### 日志脱敏

- {SensitiveDataFilter} 正则过滤 Cookie/Token/密码
- 日志中禁止打印完整 token/密码，需脱敏：`token=abc...xyz`

#### 敏感数据禁止提交

| 文件 | 约束 |
|------|------|
| `data/settings.json` | ❌ 禁止提交 Git |
| `.env` | ❌ 禁止提交 Git |
| `*.log` | ❌ 禁止提交 Git |
| `*.db` / `*.sqlite` | ❌ 禁止提交 Git |
| `*.key` / `*.pem` | ❌ 禁止提交 Git |

<!-- ============ 示例骨架结束 ============ -->

**最后更新**：2026-08-26
**当前版本**：v1.1