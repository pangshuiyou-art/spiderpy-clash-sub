# 00_模板使用说明

> **模板版本**：v1.2（2026-08-27）。本文件有**双重身份**：启用手册 + 项目侧的"版本锚点与升级凭据"——**复制到项目后必须保留，不要删除**；模板再次升级时，agent 将依据下方"模板版本历史"与本文件头部的版本号执行升级（流程见 [docs/启用与升级提示词.md](./docs/启用与升级提示词.md) 场景四）。
> **模板主副本路径**：{{模板主目录路径}}（如 `E:\PJdemo`）——启用时填入实际路径，项目日后靠它找到最新版模板。

### 模板版本历史

| 版本 | 日期 | 变更摘要 | 破坏性 |
|------|------|----------|--------|
| v1.0 | 2026-08-26 | 初版基线：AGENTS/CLAUDE/.memory/.claude hook/任务看板/附录A 只读保护/校验脚本/gitignore.example | — |
| v1.1 | 2026-08-26 | 修复三个硬伤（black 与单引号矛盾、SessionStart 钩子 schema、校验脚本误报"干净"）；新增 `.editorconfig` / `.gitattributes` / `pyproject.toml.sample`；自洽性修订（同步纪律、C 盘例外、索引追加规则、冲突提示 24 小时失效、章节显式编号）。同日并入十项评审优化：`requirements.txt.sample`、`verify_placeholders.py` 占位符校验等 | 无 |
| v1.2 | 2026-08-27 | 新增 `docs/启用与升级提示词.md`（状态判定决策树 + 四场景提示词）；本文件升级为版本中枢（启用后保留，含版本历史与主副本路径）；AGENTS.md / MEMORY.md 增加传播性指针 | **变更**：启用清单第 10 步——00_模板使用说明.md 由"复制后删除"改为"必须保留" |

本目录是一套**通用 AI Agent 协作与记忆规则模板**，适用于任何项目。复制到项目根目录后，让 AGENT 按规则建立 / 读取 / 记录记忆，并按红线清单自律。

> 本模板已吸收实战项目（DSXT）的增强反馈，自带"Git 操作纪律、修改前自检、测试纪律、长会话自查、常见陷阱、快速上手、红线写法示例"等进阶约束。全部增强来源与去留决策见 [docs/template_enhancement_inventory.md](./docs/template_enhancement_inventory.md)。
> 规则默认按 Python 项目提供（快速上手命令、CLAUDE.md §4 编码标准）；非 Python 项目复制后替换这两处即可。

## 一、这个模板是什么

| 文件/目录 | 作用 |
|-----------|------|
| `AGENTS.md` | 项目根规则的**通用入口**（适用于 Claude/Trae/WorkBuddy/Cursor/Cline/Gemini/GPT 等）。含：回复语言/结构硬规定、文件存放、记忆协议、**Git 操作纪律、禁止事项、修改前自检、测试纪律、每 5 轮自查、常见陷阱、提交规范** |
| `CLAUDE.md` | Claude 专属权威指南，编码标准唯一权威来源（§1~§5 显式编号）。含简版记忆协议、**技术栈锁定表、分层依赖方向、编码标准、项目专属红线写法示例** |
| `.memory/` | **长期记忆唯一事实源** |
| `├ MEMORY.md` | 索引总览（会话开始第一读） |
| `├ README.md` | 记忆协议全文（谁能写/写什么/格式/提交纪律） |
| `├ user/` | 用户偏好（每文件一条） |
| `├ project/` | 项目长期约束/架构决策 |
| `├ learnings/` | 踩坑/经验/bug 结论 |
| `└ archive/` | 会话流水按 `YYYY-MM/` 归档（每会话结束由当值 AGENT 更新索引） |
| `.claude/settings.json` | **选配**：Claude Code 专用 SessionStart hook，以纯文本 stdout 注入 `MEMORY.md`（无 JSON 包装，避免 schema 不符注入失效） |
| `.editorconfig` | 编辑器强制"4 空格 / LF / UTF-8"，把 CLAUDE.md §4.1 格式规则工具化 |
| `.gitattributes` | git 层强制 LF，屏蔽 Windows `autocrlf` 差异 |
| `pyproject.toml.sample` | ruff / black / pytest 工具配置（行长 120、`skip-string-normalization` 保留单引号），复制后改名 `pyproject.toml` |
| `requirements.txt.sample` | Python 依赖管理示例（严格 `包名==版本号`），复制后改名 `requirements.txt` 并按项目填写；提交前执行 `pip freeze > requirements.txt` 更新 |
| `docs/任务看板.md` | **选配**：多 AGENT 协作看板（任务认领/状态/冲突提示区 + 24 小时失效机制，不写入 `.memory/`） |
| `docs/附录A_源项目只读保护.md` | **选配**：重构复刻类项目"源项目零修改"的规则 + 校验脚本启用步骤 |
| `scripts/verify/verify_readonly.py.sample` | **选配**：源项目只读校验脚本模板（HEAD + 全量被跟踪文件哈希快照，"改完又提交"也能暴露；复制后改名去 `.sample`） |
| `scripts/verify/verify_placeholders.py` | **占位符兜底检查**：扫描模板默认关键文件中的 `{{...}}` 未替换占位符（退出码 0/1），替代 Windows 上不可靠的 `grep -rn '{{'` |
| `docs/template_enhancement_inventory.md` | **模板自身**：增强来源与去留决策的审计清单（模板维护用，复制到项目后可删除） |
| `docs/启用与升级提示词.md` | **AGENT 自助操作手册**：状态判定决策树 + 四场景提示词（新项目整套启用 / 旧项目无规则回填 / 旧项目有规则合并 / 模板升级），复制到项目后随本文件一并保留 |
| `gitignore.example` | **选配**：`.gitignore` 示例（含 `.memory/` 保留跟踪、私有目录忽略、敏感数据、源项目保护等约定） |

## 二、如何给一个新项目启用（每项目一次）

0. **若目标项目尚不是 git 仓库，先 `git init`**——记忆协议、看板协作、提交规范全部依赖 git，没有 git 这套机制跑不起来。
1. 把本目录所有文件**原样复制**到目标项目根目录（`AGENTS.md`、`CLAUDE.md`、`.memory/`、`.editorconfig`、`.gitattributes`，视需要 `.claude/` 与 `docs/`、`scripts/` 全部粘贴过去）。
2. 用编辑器打开 `AGENTS.md` 与 `CLAUDE.md`：
   - 把 `{{项目名}}`、`{{项目简介}}`、`{{包名}}` 等占位符替换为真实内容；
   - 按项目实际补写**技术栈锁定表**（约束/原因/位置三列）、**目录结构与分层**、**项目身份**、**常用模块速查**、**快速上手命令**；
   - 全文兜底：`python scripts/verify/verify_placeholders.py`（跨平台，Windows 也能用），退出码 0 表示全部替换完毕；逐个占位符确认替换完。
3. **红线写法三步**（详见第三节）。
4. 若项目有协作看板，把 `MEMORY.md` 的"看板指针"节指向 `docs/任务看板.md`；不需要多 AGENT 协作则删除看板文件与指针节。
5. 把「常见陷阱」节替换为本项目**真实踩过的坑**（查 `.memory/learnings/` 抄底），❌/✅ 对照比泛泛规则有效得多。
6. **必做**：初始化两个真实记忆条目——一条记录本次启用决策/已知约束；另一条为**采用记录** `.memory/project/YYYYMMDD-采用模板vX.Y.md`（写入采用版本号 + 模板主目录路径 + 本地偏差初盘），它是日后升级流程的事实起点。同时验证 `.memory/` 可写、可 git 提交、索引可更新。
7. （可选）若项目使用 Claude Code，保留 `.claude/settings.json`（会话开始时自动注入 `.memory/MEMORY.md`）；否则删除 `.claude/`。
8. 工具配置落地：`pyproject.toml.sample` → 改名 `pyproject.toml` 并填 `[project]` 元信息；`requirements.txt.sample` → 改名 `requirements.txt` 并按项目实际填写版本号（严格 `包名==版本号`）；`.editorconfig` / `.gitattributes` 原样保留。
9. 审视 `gitignore.example`，按需改名为 `.gitignore` 并调整忽略项（务必保留 `.memory/` 纳入 git）。
10. 删除 `docs/template_enhancement_inventory.md` 与 `AGENTS.md`/`CLAUDE.md` 中的"示例骨架"注释块；**保留 `00_模板使用说明.md` 与 `docs/启用与升级提示词.md`**（前者是版本锚点，后者是日后升级的流程依据），并把头部"模板主副本路径"占位填入实际路径。

## 三、红线怎么写（三步）

第一条红线由三个层级构成，按需求启用：

| 层级 | 内容 | 放哪 | 举例 |
|------|------|------|------|
| **通用纪律**（默认全启用） | Git 危险操作禁令、禁止事项、修改前自检、测试纪律、自查清单 | `AGENTS.md` 已内嵌，改 `{{占位}}` 即可 | "禁止 stash / reset --hard" |
| **场景红线**（按需启用） | 源项目只读保护等机制化红线 | 启用 `docs/附录A_源项目只读保护.md` + 校验脚本 | "{{源项目}} 只读，`verify_readonly.py --before`" |
| **业务红线**（每项目必填） | 本项目特有的安全/数据/流程约束 | 在 `CLAUDE.md`「项目专属红线」用示例骨架改写 | "凭证 DPAPI 加密、敏感文件禁提交" |

- 每条业务红线给「禁/允」清单 + 触发验证，别只写"要小心"。
- 只写版本不写"为什么锁"，agent 就不知道能不能动——技术栈锁定表必须带原因列。

## 四、AGENT 用起来后每天怎么工作

- **读**：会话开始先读 `.memory/MEMORY.md`；领任务先看协作看板（认领写 `【负责人】`，动文件前先加**冲突提示区**）。
- **写**：有新记忆 → `.memory/<分类>/YYYYMMDD-主题.md`（带 frontmatter）→ 更新 `MEMORY.md` 索引（只追加自己的行）→ `git commit`。
- **归档**：会话结束前，由本会话 AGENT 更新当月 `archive/YYYY-MM/session-index.md`（一句话结论）。
- **遵守**：持续记忆写 `.memory/`，禁写 AGENT 私有目录；不写密钥；commit 前 `git pull --rebase`；**修改前过一遍自检清单，测试不过不提交**。
- **长会话**：每 5 轮按 `AGENTS.md`「约束恢复提醒」自查，会话结束前过「最终检查」。

## 五、关于"强制 AGENT 遵守"的现实

任何工具都无法真正程序化"强制"一个 AGENT 读文件。最接近强制的三层：
1. 协议写在 `AGENTS.md`/`CLAUDE.md` **顶部**（多数工具会加载根目录规则文件）；
2. `.memory/MEMORY.md` 作为"会话开始不可绕过的启动点"；
3. 记忆与 git 提交绑定，违反可被 git 观测到、可回滚。

这套模板不依赖任何具体软件的专有机制，因此对**未来任何 AGENT** 都成立——这是它常青的原因。

## 六、模板演进记录

> 详细版历史见顶部"模板版本历史"表；下表为过程叙事。

- **2026-08-27 v1.2（版本感知与自助升级机制）**：
  - 新增 `docs/启用与升级提示词.md`：状态判定决策树（agent 进目录自动识别四种场景）+ 场景一~三启用提示词 + 场景四升级提示词。
  - 本文件升级为"版本中枢"：头部新增版本历史表与主副本路径占位；启用清单第 10 步改为保留本文件。
  - 传播性指针：AGENTS.md「详细规范引用」与 `.memory/MEMORY.md` 各加一行指向提示词文档，随模板复制进所有目标项目。
  - 采用记录成为启用必做项：`.memory/project/YYYYMMDD-采用模板vX.Y.md`（版本号 + 主目录路径 + 本地偏差初盘），升级闭环的事实起点。
  - 维护纪律：发布新版本 = 递增版本号 → 追加版本历史行（破坏性须附迁移说明）→ 主目录打标签 `tpl-vX.Y` → 同步更新提示词文档。
- **2026-08-26 v1.1（自查优化 + 同日十项评审优化）**：
  - 修复工具链矛盾：新增 `pyproject.toml.sample`（black `skip-string-normalization` + 行长 120），文档"单引号优先"与 `black --check` 不再打架。
  - 修复 `.claude/settings.json` 钩子：改为纯文本 stdout 注入（原 JSON 结构不符 Claude Code SessionStart schema，可能注入失效）。
  - 重写校验脚本：git 命令检查退出码（非 git 仓库不再误报"干净"）；快照升级为 HEAD + 全量被跟踪文件哈希（"改完又提交"也能暴露），记录改 JSON。
  - 自洽性：★ 四节确立 AGENTS.md 为权威源 + 双向同步纪律；C 盘红线增加"工具运行态目录例外"；记忆索引改为"各自追加、互不改动他人行"；看板冲突提示加 24 小时失效机制；CLAUDE.md 顶级章节显式编号 §1~§5。
  - 工具化：新增 `.editorconfig` / `.gitattributes`；启用清单补第 0 步 `git init`；archive 明确会话结束触发时机；模板引入版本号机制。
  - 十项评审优化（执行型 AGENT 视角）：`requirements.txt.sample`（严格包名==版本号）、`verify_placeholders.py` 跨平台占位符校验、看板增强等，详见 [分析项目模板优化点.md](./docs/分析项目模板优化点.md)。
- **2026-08-26 增强（源：DSXT 实战项目，v1.0 基线）**：
  - 内嵌通用纪律：Git 操作纪律、禁止事项、修改前自检、测试纪律、每 5 轮自查、常见陷阱（❌/✅ 示例）、技术栈锁定表（约束/原因/位置）、提交规范（type+scope+检查清单）。
  - 新增可选附录：`docs/附录A_源项目只读保护.md` + `scripts/verify/verify_readonly.py.sample`。
  - 新增模板资产：`docs/template_enhancement_inventory.md`、任务看板"冲突提示区"、gitignore 完善（敏感数据/备份/源项目保护）。
  - 原则：**通用纪律进正文，场景机制进附录，业务内容只留"写法示例"**——确保模板零业务残留，复制后填占位符即可独立使用。
- 完整去留决策与 DSXT 化映射见 [docs/template_enhancement_inventory.md](./docs/template_enhancement_inventory.md)。