# 分析项目模板优化点

> **Workspace:** e:\PJdemo

---

**User**

========================
本目录是最标准的项目模板，你先读取，然后分析还有哪里需要优化的

**Assistant**

我来先读取项目的记忆文件和目录结构，全面了解这个项目模板后再进行优化分析。

现在让我检查 AGENTS.md 和 CLAUDE.md 中是否有未处理的占位符，以及其他潜在问题。

我已完整读取了所有文件，下面给出这个项目模板的全面分析与优化方案。

---

## 大白话总结

这个项目模板整体已经非常完善了，是一套给 AI Agent 用的协作规则包，包括记忆记录、代码规范、红线纪律、工具配置等。主要发现 **3 大类共 15 项可优化点**：一是**文档自洽性**（比如两个规则文件里的四节硬规定没有同步校验清单、版本号还是占位符），二是**工具链完整性**（缺依赖管理示例、测试覆盖率配置、一键初始化脚本），三是**日常使用体验**（缺归档索引模板、任务状态不够细、没有占位符替换检查脚本）。下面逐项展开。

---

## 一、整体评价（先说好的地方）

这套模板在 v1.1 基础上已经吸收了 DSXT 实战反馈，核心亮点：

| 维度 | 现状评价 |
|------|----------|
| **记忆协议** | ✅ 完整，MEMORY.md 索引 + 四类目录 + frontmatter 规范 + git 提交纪律，设计合理 |
| **红线纪律** | ✅ 三层（通用/场景/业务）分级清晰，Git 危险操作白/黑名单、修改前自检、每 5 轮自查全部到位 |
| **格式工具链** | ✅ `.editorconfig` + `.gitattributes` + `pyproject.toml.sample` 三层落地，单引号/行长/LF 与文档一致，解决了 v1.0 的黑工具打架问题 |
| **可选附录** | ✅ 源项目只读保护的规则文档 + 校验脚本（HEAD + 全量哈希，能检测"改完又提交"），很完整 |
| **协作看板** | ✅ 冲突提示区 + 24 小时失效机制，多 Agent 并发改文件有防护 |

---

## 二、详细优化方案（按优先级排序）

### 【P0 高优】文档自洽性与占位符规范

#### 1. CLAUDE.md 末尾元数据仍是占位符

**问题**：[CLAUDE.md#L233-L234](file:///e:/PJdemo/CLAUDE.md#L233-L234) 中：
```
**最后更新**：{{更新日期}}
**当前版本**：v{{版本号}}
```
而 [00_模板使用说明.md#L3](file:///e:/PJdemo/00_模板使用说明.md#L3) 已经写了版本是 `v1.1（2026-08-26）`，两处存在信息分裂。

**优化**：将 CLAUDE.md 末尾替换为真实版本号：
```markdown
**最后更新**：2026-08-26
**当前版本**：v1.1
```

---

#### 2. AGENTS.md 末尾同样是占位符

**问题**：[AGENTS.md#L337](file:///e:/PJdemo/AGENTS.md#L337) 中 `**最后更新**：{{模板升级日期}}`。

**优化**：同样改为 `2026-08-26`，与使用说明和 CLAUDE.md 保持一致。

---

#### 3. ★ 四节硬规定的"双向同步纪律"缺少落地校验清单

**问题**：[AGENTS.md#L9-L10](file:///e:/PJdemo/AGENTS.md#L9-L10) 和 [CLAUDE.md#L6-L7](file:///e:/PJdemo/CLAUDE.md#L6-L7) 都写了「修改任一份时必须同步另一份」，但没有给出**具体哪四节、怎么校验**，人工对照很容易漏。

**优化**：在 `docs/` 下新增一份**同步对照表**（或嵌入 00_模板使用说明），明确四节的位置对应关系：

| 四节名称 | AGENTS.md 位置 | CLAUDE.md 位置 | 同步校验要点 |
|----------|---------------|----------------|-------------|
| 回复语言硬性规定 | §★1（#L16-#L22 附近） | §★1 | 5 条规则逐条一致 |
| 回复结构硬性规定 | §★2 | §★2 | 5 条规则逐条一致 |
| 文件存放硬性规定 | §★3 | §★3 | 7 条规则逐条一致 |
| 记忆协议（必读必守） | §★4 | §★4 | 4+ 条规则逐条一致 |

并在 AGENTS.md / CLAUDE.md 的「同步纪律」行尾追加：
> （同步对照清单见 `docs/四节同步对照表.md`，修改后请逐项勾选）

---

### 【P0 高优】工具链与脚本缺失

#### 4. 缺少 `requirements.txt.sample` 依赖管理示例

**问题**：用户规则明确要求「固定 Python 3.12.10、DrissionPage 4.1.0.3」「依赖统一通过 requirements.txt 管理，严格遵循包名==版本号」，但模板中只有 `pyproject.toml.sample`（用于工具配置），**缺少 Python 依赖的示例文件**。新项目启用时不知道要装什么。

**优化**：新增 `requirements.txt.sample`：
```txt
# Python 依赖管理示例 —— 复制后改名 requirements.txt，按实际增删
# 规则：严格 包名==版本号；提交前执行 pip freeze > requirements.txt 更新
# ===== 核心依赖（按项目替换）=====
# drissionpage==4.1.0.3
# requests==2.32.3
# pydantic==2.9.2

# ===== 开发/测试依赖 =====
ruff==0.6.8
black==24.8.0
pytest==8.3.3
pytest-cov==5.0.0
```

并在 `00_模板使用说明.md` 第二节启用清单的第 8 步（工具配置落地）后追加：
> 8.1 依赖管理：`requirements.txt.sample` → 改名 `requirements.txt`，按项目实际填写版本号。

---

#### 5. pyproject.toml.sample 缺少 pytest-cov 配置与 mypy 可选配置

**问题**：[pyproject.toml.sample#L24-L25](file:///e:/PJdemo/pyproject.toml.sample#L24-L25) 只有 `testpaths = ["tests"]`，但 AGENTS.md 快速上手明确要求 `pytest --cov={{包名}}`，缺少覆盖率的配置（如覆盖率阈值、报告格式、排除路径）。

**优化**：在 [pyproject.toml.sample](file:///e:/PJdemo/pyproject.toml.sample) 追加：
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
# 覆盖率配置（--cov 命令行参数已够用，这里兜底默认）
addopts = "-ra --strict-markers"

# [tool.coverage.run]
# source = ["{{包名}}"]
# omit = [
#     "{{包名}}/*_test.py",
#     "tests/*",
# ]

# [tool.coverage.report]
# show_missing = true
# skip_covered = false
# # 建议阈值按项目要求设，这里给参考：用户规则≥70%
# fail_under = 70
# exclude_lines = [
#     "pragma: no cover",
#     "if TYPE_CHECKING:",
#     "raise NotImplementedError",
# ]

# ===== 可选：类型检查（若启用 mypy）=====
# [tool.mypy]
# python_version = "3.12"
# strict = true
# warn_return_any = true
# warn_unused_configs = true
# disallow_untyped_defs = true
# ignore_missing_imports = false
```

---

#### 6. 缺少「一键占位符检查脚本」

**问题**：[00_模板使用说明.md#L40](file:///e:/PJdemo/00_模板使用说明.md#L40) 要求「全文兜底：`grep -rn '{{' AGENTS.md CLAUDE.md`」，但 Windows 上 grep 不一定有（PowerShell 用 `Select-String`），新用户启用模板时容易遗漏检查。

**优化**：新增 `scripts/verify/verify_placeholders.py`，跨平台检查项目中剩余的 `{{...}}` 占位符：
```python
"""启用模板时的占位符兜底检查脚本。

遍历指定文件（默认 AGENTS.md、CLAUDE.md、pyproject.toml.sample、
docs/*.md、scripts/**/*.sample），输出所有未替换的 {{...}} 占位符，
并给出退出码（0=无占位符，1=存在占位符）。
"""

from __future__ import annotations

import re
import sys
from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parents[2]

# 默认检查范围（按模板结构设定，启用后可按需扩充）
DEFAULT_PATTERNS: list[str] = [
    'AGENTS.md',
    'CLAUDE.md',
    'pyproject.toml.sample',
    'docs/**/*.md',
    'scripts/**/*.sample',
    'scripts/**/*.py',
    '.memory/**/*.md',
]

PLACEHOLDER_RE = re.compile(r'\{\{[^}]+\}\}')


def collect_files() -> list[Path]:
    files: set[Path] = set()
    for pattern in DEFAULT_PATTERNS:
        for path in PROJECT_ROOT.glob(pattern):
            if path.is_file():
                files.add(path)
    return sorted(files)


def scan_file(file_path: Path) -> list[tuple[int, str]]:
    matches: list[tuple[int, str]] = []
    for line_no, line in enumerate(
        file_path.read_text(encoding='utf-8').splitlines(), start=1
    ):
        for match in PLACEHOLDER_RE.findall(line):
            matches.append((line_no, match))
    return matches


def main() -> int:
    files = collect_files()
    total_hits = 0
    print(f'检查 {len(files)} 个文件中是否存在未替换的 {{...}} 占位符...\n')

    for file_path in files:
        hits = scan_file(file_path)
        if not hits:
            continue
        rel = file_path.relative_to(PROJECT_ROOT)
        print(f'[FOUND] {rel}')
        for line_no, match in hits:
            print(f'  L{line_no:<4} {match}')
            total_hits += 1

    if total_hits == 0:
        print('✅ 未发现占位符，模板已完整启用。')
        return 0
    print(f'\n❌ 共发现 {total_hits} 处未替换占位符，请按上列清单处理。')
    return 1


if __name__ == '__main__':
    sys.exit(main())
```

并在 `00_模板使用说明.md` 第二节第 2 步后追加：
> 2.1 占位符兜底检查：`python scripts/verify/verify_placeholders.py`，退出码 0 表示全部替换完毕。

---

#### 7. verify_readonly.py.sample 缺少「配置未修改」自检

**问题**：[verify_readonly.py.sample#L29-L30](file:///e:/PJdemo/scripts/verify/verify_readonly.py.sample#L29-L30) 两处仍是 `{{源项目绝对路径}}` 占位。如果用户复制后忘记改，`SOURCE_ROOT.exists()` 会报"目录不存在"，但**没有明确提示"你还没改脚本顶部配置"**。

**优化**：在 `main()` 函数开头，`SOURCE_ROOT.exists()` 检查前，先增加一个**占位符自检**：

```python
def _config_unset() -> bool:
    """检测用户是否还没修改脚本顶部的 {{...}} 占位配置。"""
    return '{{' in str(SOURCE_ROOT) or '{{' in str(HASH_RECORD)


def main() -> int:
    parser = argparse.ArgumentParser(description='源项目只读保护校验')
    ...  # 原有参数定义
    args = parser.parse_args()

    if _config_unset():
        print('[FAIL] 脚本顶部 SOURCE_ROOT / HASH_RECORD 仍是 {{...}} 占位符。')
        print('请先编辑脚本顶部两处配置后再运行。')
        return 1

    if not SOURCE_ROOT.exists():
        ...  # 原有
```

---

### 【P1 中优】记忆协议体验

#### 8. archive/ 缺少 `YYYY-MM/session-index.md` 模板

**问题**：[.memory/archive/README.md](file:///e:/PJdemo/.memory/archive/README.md) 要求「每个会话结束前写入/更新当月 `YYYY-MM/session-index.md`」，但没有给出文件格式模板，Agent 第一次写时格式不统一。

**优化**：新增 `.memory/archive/session-index.template.md`：
```markdown
# 会话流水索引 YYYY-MM

> 本文件由当月各会话 AGENT 在会话结束前追加。格式：一行一条，追加到下表末尾。

| 日期 | 会话摘要（一句话结论） | 归档 / 报告链接 |
|------|----------------------|----------------|
| 2026-08-26 | 示例：初始化模板，完成记忆协议与红线配置 | docs/phase1_init.md |
```

并在 [.memory/archive/README.md](file:///e:/PJdemo/.memory/archive/README.md) 末尾追加：
> **格式模板**：首次写某月份时，复制 `archive/session-index.template.md` 为 `archive/YYYY-MM/session-index.md`，删除示例行后追加。

---

#### 9. .memory/project/ 和 .memory/user/ 缺少第一条示范性条目

**问题**：当前 `project/` 和 `user/` 目录是空的（只有 README），首次启用模板的用户不知道"什么内容应该写进来"。learnings 目录已有一条示范（格式化工具冲突），但 project/user 没有。

**优化**：新增两条示范性条目（启用说明明确告知"保留或替换为自己的"）：

**`.memory/project/20260826-模板启用决策.md`**：
```markdown
---
name: 模板启用决策
description: 本项目基于 AI 协作模板 v1.1 复制，记录启用时的初始决策
type: project
created: 2026-08-26
updated: 2026-08-26
---

## 模板基线
- 基于 `e:\PJdemo` 模板 v1.1（2026-08-26）复制
- 已按启用清单完成占位符替换、工具配置落地
- （按需勾选：启用了源项目只读保护 / 启用了多 AGENT 协作看板）

## 初始架构决策
- 分层：（按项目填写，如 api → services → core → db）
- 技术栈锁定：见 CLAUDE.md §2 / AGENTS.md「核心约束 1」
- （填项目实际的、不能从代码推导的决策，如数据一致性规则、幂等策略）
```

**`.memory/user/20260826-用户偏好默认值.md`**：
```markdown
---
name: 用户偏好默认值
description: 用户与 AGENT 协作的默认偏好（有新约定时覆盖更新）
type: user
created: 2026-08-26
updated: 2026-08-26
---

- 回复语言：中文
- 工作方式：重要改动先给方案 → 确认后再动工
- 测试要求：测试失败立即停止、报告原因、等待指示
- 交付要求：自行验证无 Bug，交付可直接部署
```

并在 `00_模板使用说明.md` 第二节第 6 步后追加：
> 6.1 project/ 与 user/ 目录已附带示范性条目（`20260826-模板启用决策.md`、`20260826-用户偏好默认值.md`），请替换为项目实际内容或保留。

---

### 【P1 中优】协作看板增强

#### 10. 任务看板缺少「阻塞/依赖」字段与状态枚举细化

**问题**：[任务看板.md](file:///e:/PJdemo/docs/任务看板.md) 的任务表格只有「负责人/状态/说明」三列。实际协作中经常遇到"任务 B 依赖任务 A 完成"或"任务因外部原因阻塞"的场景，没有地方记录。状态也只是自由文本，没有统一枚举。

**优化**：任务表格增加两列，并在「使用约定」中补充状态枚举：

```markdown
## 使用约定
...（原内容保留）

- **状态枚举（统一，避免自由文本）**：`待认领` → `处理中` → `【阻塞】原因` → `待验证` → `已完成` / `已取消`
- **依赖**：「阻塞」状态时必填阻塞原因，如"依赖 #3 完成"、"等待用户确认 xxx"

## 模块一：xxx【负责人: 】【状态: 】>

### 任务列表

| 编号 | 优先级 | 事项 | 负责人 | 状态 | 依赖/阻塞 | 说明/结论 |
|------|--------|------|--------|------|-----------|-----------|
| 1    | P1     | 示例 | __     | 待认领 | —        | 描述 |
```

---

#### 11. 看板缺少「冲突提示区」集中展示

**问题**：冲突提示是「写在所属条目下」，当多个条目同时有提示时，Agent 扫一眼看不到全局谁在改哪些文件。

**优化**：在看板顶部增加一个**集中冲突提示速览区**：

```markdown
---

## ⚠️ 当前冲突提示速览（24h 有效，过期自动失效）

| 条目 | 改动文件范围 | 提示时间 | 负责人 |
|------|-------------|----------|--------|
| 模块一 #1 | services/login.py、api/router.py | 2026-08-26 10:30 | __ |

---
```

并在「使用约定」第 14-16 行追加：
> 同时将同一条信息追加到顶部「⚠️ 当前冲突提示速览」表中，便于全局查看。完成并验证后，两处同步移除。

---

### 【P2 低优】边缘细节完善

#### 12. .editorconfig 缺少常见文件类型配置

**问题**：当前 [.editorconfig](file:///e:/PJdemo/.editorconfig) 只有 `*`、`*.md`、`*.{yml,yaml,json,toml}`，遗漏了日常开发中常见的格式敏感文件。

**优化**：追加：
```ini
[*.{py,js,ts,tsx}]
indent_size = 4

[Makefile]
indent_style = tab   # Makefile 必须用 tab，不能是 4 空格
indent_size = 4

[Dockerfile]
indent_size = 4

[*.{sh,bash}]
indent_size = 4
end_of_line = lf     # 脚本强制 LF，避免 Windows 写的 shell 脚本在 Linux 上报错
```

---

#### 13. .gitattributes 二进制列表可扩充

**问题**：[.gitattributes](file:///e:/PJdemo/.gitattributes) 只列了 8 种二进制。实际项目还会遇到很多常见二进制格式。

**优化**：追加：
```gitattributes
# 图片
*.webp binary
*.svg binary
*.bmp binary
*.tif binary
*.tiff binary

# 文档
*.doc binary
*.docx binary
*.xls binary
*.xlsx binary
*.ppt binary
*.pptx binary
*.odt binary
*.ods binary

# 字体
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
*.eot binary

# 压缩包
*.tar binary
*.gz binary
*.7z binary
*.rar binary

# 其他
*.class binary
*.jar binary
*.war binary
```

---

#### 14. .gitignore 与 gitignore.example 缺少 data/、logs/ 目录的忽略

**问题**：两个 gitignore 都有 `*.log`、`*.db`、`data/settings.json`，但**目录本身** `data/`、`logs/` 的默认忽略缺失（如果目录下有子目录结构，可能意外提交空目录占位文件或测试数据）。

**优化**：在 [.gitignore](file:///e:/PJdemo/.gitignore) 和 [gitignore.example](file:///e:/PJdemo/gitignore.example) 的「日志」和「敏感数据」节分别追加：

```
## 运行时数据目录（默认整目录忽略，需要跟踪时反向取消）
data/
logs/
!data/.gitkeep
!logs/.gitkeep
```

并在两个文件开头加一条注释：
```
# 如需跟踪 data/ 或 logs/ 下的特定子目录/文件，在下方加 !data/xxx/ 形式的反向规则。
```

---

#### 15. `.claude/settings.json` 的注入命令缺少异常处理

**问题**：当前 [.claude/settings.json#L9](file:///e:/PJdemo/.claude/settings.json#L9) 的命令是：
```python
python -c "import os,sys; root=os.environ.get('CLAUDE_PROJECT_DIR') or os.getcwd(); sys.stdout.write(open(os.path.join(root,'.memory','MEMORY.md'),encoding='utf-8').read())"
```
如果 `.memory/MEMORY.md` 不存在（比如模板刚复制、`.memory/` 目录丢失），会抛 `FileNotFoundError`，整个 SessionStart hook 失败，可能影响 Claude Code 启动。

**优化**：加 try/except，失败时静默输出友好提示而不是抛异常：
```json
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import os,sys; root=os.environ.get('CLAUDE_PROJECT_DIR') or os.getcwd(); p=os.path.join(root,'.memory','MEMORY.md');\\ntry:\\n  sys.stdout.write(open(p,encoding='utf-8').read())\\nexcept FileNotFoundError:\\n  sys.stdout.write('[提示] .memory/MEMORY.md 不存在，请按模板启用说明初始化记忆目录。')\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

---

## 三、优化前后对照表（总览）

| # | 优化项 | 优先级 | 新增/修改文件 |
|---|--------|--------|-------------|
| 1 | CLAUDE.md 末尾版本号填空 | P0 | 修改 `CLAUDE.md` |
| 2 | AGENTS.md 末尾更新日期填空 | P0 | 修改 `AGENTS.md` |
| 3 | 四节硬规定同步对照表 | P0 | 新增 `docs/四节同步对照表.md` + 修改 AGENTS/CLAUDE 两处 |
| 4 | requirements.txt.sample | P0 | 新增 |
| 5 | pyproject.toml.sample 覆盖率 + mypy 配置 | P0 | 修改 |
| 6 | verify_placeholders.py 占位符检查脚本 | P0 | 新增 `scripts/verify/verify_placeholders.py` |
| 7 | verify_readonly.py.sample 顶部配置自检 | P0 | 修改 |
| 8 | archive session-index 模板 | P1 | 新增 `.memory/archive/session-index.template.md` + 修改 archive/README.md |
| 9 | project/ user/ 首条示范性条目 | P1 | 各新增 1 个 md 文件 |
| 10 | 看板状态枚举 + 依赖列 | P1 | 修改 `docs/任务看板.md` |
| 11 | 看板顶部集中冲突提示速览区 | P1 | 修改 `docs/任务看板.md` |
| 12 | .editorconfig 常见文件类型补齐 | P2 | 修改 |
| 13 | .gitattributes 二进制扩充 | P2 | 修改 |
| 14 | .gitignore data/logs 目录忽略 | P2 | 修改 `.gitignore` + `gitignore.example` |
| 15 | .claude/settings.json 注入容错 | P2 | 修改 |

---

## 四、不建议改动的部分（避免过度优化）

为遵循「功能保全原则」，以下部分**刻意保留不动**：

| 内容 | 不动原因 |
|------|----------|
| 所有 `{{...}}` 占位符 | 这是模板设计意图，启用时由用户替换；优化 6 已提供自动检查工具 |
| AGENTS.md / CLAUDE.md 的「示例骨架注释块」 | 使用说明第 10 步明确告知启用时删除，保留是为了让用户看到"怎么填" |
| `docs/template_enhancement_inventory.md` | 是模板自身的审计资产，非项目产物，使用说明第 10 步明确告知删除 |
| verify_readonly.py.sample 的核心校验逻辑（HEAD + 全量哈希） | 已能覆盖"改完又提交"场景，逻辑健壮 |

---

以上就是完整的优化分析。需要我直接实施以上哪些优化项？可以按优先级分批执行（建议先 P0 共 7 项）。

分析工作已完成。以上方案共 **15 项优化**，覆盖 3 个优先级，其中 **P0 共 7 项为建议必改项**（文档自洽性 + 工具链缺失）。如果确认方案合理，我可以立即开始实施。

---

**User**

你要基于AGENT执行的方式，认为你的方案是否合理，再进行修改

**Assistant**

我先从「执行型 AGENT」的视角重新评审方案，再动手修改。

## 大白话总结

重新评审后发现：原方案 15 项里有 **5 项不合理**，核心问题是“站在维护者视角想当然”，没站在真正干活的 AGENT 视角。比如：新建“四节同步对照表”是给改模板的人看的，执行任务的 agent 根本用不上；往 `.memory/project/`、`user/` 塞**假示范条目**反而会污染复制出去的新项目记忆库；`.gitignore` 整目录忽略 `data/` 会误伤需要跟踪数据文件的项目。真正高价值的是**把规则落到工具层**（覆盖率阈值、占位符检查、hook 容错），因为 agent 遵守文档靠自觉、遵守工具靠退出码。修订后方案从 15 项收敛为 10 项，现在开始实施。

## 评审结论（逐项判定）

| 原项 | 判定 | 理由（agent 执行视角） |
|------|------|----------------------|
| 1/2 版本号填空 | ✅ 保留 | 成本 1 行，消除“模板自身有未替换占位符”的坏示范 |
| 3 四节同步对照表 | ❌ **取消** | 服务模板维护者，不服务执行 agent；且多一份要维护同步的文档，本身违反模板自己的“防漂移”原则 |
| 4 requirements.txt.sample | ✅ 简化保留 | agent 执行 `pip install -r` 和“pip freeze 更新”纪律需要明确入口 |
| 5 coverage + mypy | ✅ **提级** | 这是最符合已有 learning（“规则必须落到工具层”）的项：`fail_under=70` 让覆盖率不达标时 pytest 退出码非 0，agent“测试不过不提交”从自觉变成强制 |
| 6 占位符检查脚本 | ✅ 保留 | 替代 Windows 上不可靠的 `grep -rn` 指令，agent 启用模板时直接跑脚本 |
| 7 脚本配置自检 | ✅ 保留 | 真实失败模式：忘改 `{{源项目绝对路径}}` 时给出精准提示 |
| 8 session-index 模板 | 🔄 **改为内嵌** | 不新增文件，直接在 archive/README.md 里给格式示例——避免文件膨胀 |
| 9 假示范条目 | ❌ **取消** | 模板说明书已要求启用时“初始化真实条目”；预置假条目会被原样复制进新项目，污染记忆库，agent 可能误读为真实约束 |
| 10/11 看板增强 | 🔄 **简化** | 状态枚举+依赖列保留；冲突提示**集中到顶部一处**（原方案“条目下+顶部双写”必然漂移，agent 会忘删其中一处） |
| 12 .editorconfig | 🔄 精简 | 只保留 Makefile(tab) 和 *.sh(LF) 两条真实坑，删掉大段低频配置 |
| 13 .gitattributes 扩充 | ❌ **取消** | 现有 8 类已覆盖高频场景，扩充是纯文件膨胀 |
| 14 整目录忽略 data/logs | ❌ **取消** | 现有按模式忽略（settings.json/*.db/*.log）更安全；整目录忽略会误伤需要跟踪 `data/config.json` 之类的项目 |
| 15 hook 容错 | ✅ 保留 | 真实失败模式：MEMORY.md 缺失导致 SessionStart hook 崩溃，影响 Claude Code 启动 |

现在开始实施。先确认 git 状态。

---

